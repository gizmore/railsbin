<p id="notice"><%= notice %></p>

<h1>Listing Pastes</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Title</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pastes.each do |paste| %>
      <tr>
        <td><%= paste.display_creator %></td>
        <td><%= paste.title %></td>
        <td><%= paste.content[0..64] %></td>
        <td><%= link_to 'Show', paste %></td>
        <td><%= link_to 'Edit', edit_paste_path(paste) %></td>
        <td><%= link_to 'Destroy', paste, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Paste', new_paste_path %>
